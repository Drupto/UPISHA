[build]
  # Gate deploys on typecheck — a type error blocks the production build
  command = "npm run typecheck && next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
